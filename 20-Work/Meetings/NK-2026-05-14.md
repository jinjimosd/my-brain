
chi phí: duyệt theo cấp, mỗi cấp chỉ cần 1 ng duyệt
SAP-JPH: luồng bên khác đẩy sang, có status xem hoàn thành chưa


Office 365 sync từ ad local lên

trường attribute13: sync office 365
trường attribute4: value mặc định nk

\\ttmssg.com\root$


Tới đây triển khai con cvoice mới, hiện đang xài chung CRV.
Chưa có domain mới.

lưu ý kiến khách hàng vào database, chưa biết db nào
nếu đủ đk thì gửi vào channel tele

DB postgres mới nhất:
frontend public, backend public

team data lấy dữ liệu ra để lấy ra sự hài lòng.
sau khi dựng mới, import db, trỏ domain thì có cần liên hệ bên nào không, ví dụ như bên team data
powerbi này thì nk có không hay là của bên crv
hiện đang dựng trong k8s hay deploy trên server

dashboard phân quyền cho một số bộ phận 
dashboard strapi chỉ có dev thấy


có mấy môi trường
docker, k8s hay trên server










